% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarizers.R
\name{top_transitions}
\alias{top_transitions}
\title{Top Career Transitions}
\usage{
top_transitions(
  model_data,
  by = c("sector", "region"),
  n = 10,
  weight = "transition_weighted_count"
)
}
\arguments{
\item{model_data}{A tibble containing career transition data (e.g., from load_cmap_data()).}

\item{by}{Character vector specifying grouping variables. Default is c("sector", "region").}

\item{n}{Integer specifying the number of top transitions to return per group. Default is 10.}

\item{weight}{Character string specifying the weight column to rank by. Default is "transition_weighted_count".}
}
\value{
A tibble with the top N transitions per group, ranked by weight.
}
\description{
Returns the top N transitions by weight within each group.
}
\details{
This function groups the model_data by the specified variables and returns the top N rows
(ranked by the weight column) for each group.
}
\examples{
\dontrun{
result <- load_cmap_data(base_path = "~/cmap_data")
top <- top_transitions(result$model_data, by = "sector", n = 5)
}
}
